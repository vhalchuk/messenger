# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# When adding additional env variables, the schema in /env/schema.mjs should be updated accordingly

# Google OAuth Client ID and Secret, used for authentication
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Database URL, used for connecting to your database
DATABASE_URL=
# NextAuth.js secret key, used for signing and encrypting session data
# Can be generated by the following command: `openssl rand -hex 64`
NEXTAUTH_SECRET=
# Next.js URL, used for generating URLs in server-side code
NEXTAUTH_URL=http://localhost:3000
# Public API URL, used for making API requests from the client
NEXT_PUBLIC_API_URI=http://localhost:4000/graphql
# Public WebSocket URL, used for subscriptions from the client
NEXT_PUBLIC_WS_URI=ws://localhost:4000/graphql/subscriptions
