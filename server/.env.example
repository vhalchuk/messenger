# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# The origin of the client application, used for CORS protection
CLIENT_ORIGIN=http://localhost:3000
# The URL of the Next.js application, used for generating URLs in server-side code
NEXTAUTH_URL=http://localhost:3000
# The URL of the MongoDB database, used for storing data
DATABASE_URL=
# The secret key used by NextAuth.js to sign and encrypt session data
NEXTAUTH_SECRET=
# Google OAuth client ID, used for authenticating users with Google
GOOGLE_CLIENT_ID=
# Google OAuth client secret, used for authenticating users with Google
GOOGLE_CLIENT_SECRET=
